<main>
	<div class="bg-grid"></div>
	<div class="bg-decorations">
		<div class="corner corner-tl"></div>
		<div class="corner corner-tr"></div>
		<div class="corner corner-bl"></div>
		<div class="corner corner-br"></div>
	</div>

	<div class="glow-orb orb-1"></div>

	<div class="name-container">
		<div class="name-row">
			<span class="first-name">CAIO</span>
			<span class="divider"></span>
		</div>
		<span class="last-name">TEIXEIRA</span>
	</div>

	<div class="info-section">
		<div class="decoration-line">
			<span class="dot"></span>
			<span class="line"></span>
			<span class="line-vertical"></span>
			<span class="diamond"></span>
			<span class="line-vertical"></span>
			<span class="line"></span>
			<span class="dot"></span>
		</div>

		<div class="role">
			<span class="role-line"></span>
			<span class="role-text">FULLSTACK DEVELOPER</span>
			<span class="role-line"></span>
		</div>
	</div>

	<div class="scroll-indicator">
		<div class="scroll-line"></div>
		<div class="scroll-dot"></div>
		<span class="scroll-text">SCROLL</span>
	</div>
</main>

<script>
	const scrollIndicator = document.querySelector('.scroll-indicator');
	
	window.addEventListener('scroll', () => {
		if (window.scrollY > 100) {
			scrollIndicator?.classList.add('hidden');
		} else {
			scrollIndicator?.classList.remove('hidden');
		}
	});
</script>

<style>
	main {
		width: 100%;
		min-height: 100vh;
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		gap: 3rem;
		position: relative;
		background-color: #ffffff;
		overflow: hidden;
		scroll-snap-align: start;
	}

	.glow-orb {
		position: absolute;
		border-radius: 50%;
		pointer-events: none;
		z-index: 0;
		filter: blur(80px);
		animation: orb-float 15s ease-in-out infinite;
	}

	.orb-1 {
		width: 400px;
		height: 400px;
		background: radial-gradient(circle, #e6394610 0%, transparent 70%);
		top: 30%;
		left: 20%;
	}

	@keyframes orb-float {
		0%, 100% { transform: translate(0, 0); opacity: 0.5; }
		50% { transform: translate(30px, -20px); opacity: 0.7; }
	}

	.bg-grid {
		position: absolute;
		inset: 0;
		background-image: 
			linear-gradient(to right, #00000008 1px, transparent 1px),
			linear-gradient(to bottom, #00000008 1px, transparent 1px);
		background-size: 80px 80px;
		background-position: center center;
		pointer-events: none;
		z-index: 0;
	}

	.bg-decorations {
		position: absolute;
		inset: 0;
		pointer-events: none;
		z-index: 0;
	}

	.corner {
		position: absolute;
		width: 60px;
		height: 60px;
		border-color: #e6394640;
		border-style: solid;
		border-width: 0;
		transition: all 0.3s ease;
	}

	.corner::before {
		content: '';
		position: absolute;
		width: 8px;
		height: 8px;
		background: #e63946;
		clip-path: polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%);
		animation: corner-pulse 3s ease-in-out infinite;
	}

	@keyframes corner-pulse {
		0%, 100% { opacity: 0.3; transform: scale(1); }
		50% { opacity: 0.8; transform: scale(1.2); }
	}

	.corner-tl {
		top: 20px;
		left: 20px;
		border-top-width: 1px;
		border-left-width: 1px;
		animation: corner-tl-draw 1s ease-out forwards;
	}

	.corner-tl::before {
		top: -4px;
		left: -4px;
		animation-delay: 0s;
	}

	.corner-tr {
		top: 20px;
		right: 20px;
		border-top-width: 1px;
		border-right-width: 1px;
		animation: corner-tr-draw 1s ease-out forwards 0.2s;
	}

	.corner-tr::before {
		top: -4px;
		right: -4px;
		animation-delay: 0.5s;
	}

	.corner-bl {
		bottom: 20px;
		left: 20px;
		border-bottom-width: 1px;
		border-left-width: 1px;
		animation: corner-bl-draw 1s ease-out forwards 0.4s;
	}

	.corner-bl::before {
		bottom: -4px;
		left: -4px;
		animation-delay: 1s;
	}

	.corner-br {
		bottom: 20px;
		right: 20px;
		border-bottom-width: 1px;
		border-right-width: 1px;
		animation: corner-br-draw 1s ease-out forwards 0.6s;
	}

	.corner-br::before {
		bottom: -4px;
		right: -4px;
		animation-delay: 1.5s;
	}

	@keyframes corner-tl-draw {
		0% { width: 0; height: 0; opacity: 0; }
		100% { width: 60px; height: 60px; opacity: 1; }
	}

	@keyframes corner-tr-draw {
		0% { width: 0; height: 0; opacity: 0; }
		100% { width: 60px; height: 60px; opacity: 1; }
	}

	@keyframes corner-bl-draw {
		0% { width: 0; height: 0; opacity: 0; }
		100% { width: 60px; height: 60px; opacity: 1; }
	}

	@keyframes corner-br-draw {
		0% { width: 0; height: 0; opacity: 0; }
		100% { width: 60px; height: 60px; opacity: 1; }
	}

	.name-container {
		display: flex;
		flex-direction: column;
		align-items: center;
		text-align: center;
		position: relative;
		z-index: 1;
		margin-left: -80px;
	}

	.info-section {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 1.5rem;
		position: relative;
		z-index: 1;
		margin-left: -80px;
	}

	.scroll-indicator {
		position: absolute;
		bottom: 2rem;
		left: calc(50% - 40px);
		transform: translateX(-50%);
		display: flex;
		z-index: 10;
		flex-direction: column;
		align-items: center;
		gap: 0.5rem;
		opacity: 1;
		transition: opacity 0.3s ease;
		animation: scroll-fade-in 1s ease-out forwards 2s;
	}

	@keyframes scroll-fade-in {
		0% { opacity: 0; transform: translateX(-50%) translateY(20px); }
		100% { opacity: 1; transform: translateX(-50%) translateY(0); }
	}

	.scroll-indicator.hidden {
		opacity: 0;
		pointer-events: none;
	}

	.name-row {
		display: flex;
		align-items: stretch;
		justify-content: center;
	}

	.first-name {
		font-size: 8rem;
		font-weight: 900;
		color: #1a1a1a;
		letter-spacing: -0.02em;
		line-height: 1;
		position: relative;
		animation: name-reveal 1.2s cubic-bezier(0.77, 0, 0.175, 1) forwards;
		text-shadow: 2px 2px 0 #e6394610;
	}

	.first-name::after {
		content: 'CAIO';
		position: absolute;
		top: 0;
		left: 0;
		color: transparent;
		-webkit-text-stroke: 1px #e6394620;
		animation: name-glitch 4s ease-in-out infinite 2s;
	}

	@keyframes name-reveal {
		0% { 
			opacity: 0; 
			transform: translateY(30px);
			filter: blur(10px);
		}
		100% { 
			opacity: 1; 
			transform: translateY(0);
			filter: blur(0);
		}
	}

	@keyframes name-glitch {
		0%, 100% { transform: translate(0, 0); opacity: 0; }
		2% { transform: translate(2px, -2px); opacity: 0.5; }
		4% { transform: translate(-2px, 2px); opacity: 0.3; }
		6% { transform: translate(0, 0); opacity: 0; }
	}

	.divider {
		width: 5px;
		background: linear-gradient(180deg, #e63946 0%, #e6394680 50%, #e63946 100%);
		margin-left: 1rem;
		align-self: stretch;
		position: relative;
		animation: divider-grow 0.8s ease-out forwards 0.5s;
		transform-origin: center;
		transform: scaleY(0);
	}

	.divider::before {
		content: '';
		position: absolute;
		top: -8px;
		left: 50%;
		transform: translateX(-50%);
		width: 12px;
		height: 12px;
		background: #e63946;
		clip-path: polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%);
		animation: diamond-pulse 2s ease-in-out infinite;
	}

	.divider::after {
		content: '';
		position: absolute;
		bottom: -8px;
		left: 50%;
		transform: translateX(-50%);
		width: 12px;
		height: 12px;
		background: #e63946;
		clip-path: polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%);
		animation: diamond-pulse 2s ease-in-out infinite 1s;
	}

	@keyframes divider-grow {
		0% { transform: scaleY(0); }
		100% { transform: scaleY(1); }
	}

	@keyframes diamond-pulse {
		0%, 100% { transform: translateX(-50%) scale(1); opacity: 1; }
		50% { transform: translateX(-50%) scale(1.2); opacity: 0.7; }
	}

	.last-name {
		font-size: 3.2rem;
		font-weight: 400;
		color: #666;
		letter-spacing: 0.35em;
		text-transform: uppercase;
		margin-top: 0.5rem;
		position: relative;
		animation: lastname-slide 1s ease-out forwards 0.8s;
		opacity: 0;
	}

	.last-name::before {
		content: '';
		position: absolute;
		bottom: -5px;
		left: 0;
		width: 100%;
		height: 1px;
		background: linear-gradient(90deg, transparent, #e6394650, transparent);
		animation: line-expand 1.5s ease-out forwards 1.5s;
		transform: scaleX(0);
	}

	@keyframes lastname-slide {
		0% { opacity: 0; transform: translateX(-20px); }
		100% { opacity: 1; transform: translateX(0); }
	}

	@keyframes line-expand {
		0% { transform: scaleX(0); }
		100% { transform: scaleX(1); }
	}

	.decoration-line {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		animation: fade-in 1s ease-out forwards 1.2s;
		opacity: 0;
	}

	@keyframes fade-in {
		0% { opacity: 0; transform: translateY(10px); }
		100% { opacity: 1; transform: translateY(0); }
	}

	.dot {
		width: 6px;
		height: 6px;
		background-color: #e6394650;
		border-radius: 50%;
		animation: dot-blink 3s ease-in-out infinite;
	}

	@keyframes dot-blink {
		0%, 100% { opacity: 0.5; transform: scale(1); }
		50% { opacity: 1; transform: scale(1.2); box-shadow: 0 0 8px #e6394680; }
	}

	.line {
		width: 40px;
		height: 1px;
		background: linear-gradient(90deg, #e6394630, #e6394680);
		position: relative;
		overflow: hidden;
	}

	.line::after {
		content: '';
		position: absolute;
		top: 0;
		left: -100%;
		width: 100%;
		height: 100%;
		background: linear-gradient(90deg, transparent, #e63946, transparent);
		animation: line-shine 3s linear infinite;
	}

	@keyframes line-shine {
		0% { left: -100%; }
		100% { left: 200%; }
	}

	.line-vertical {
		width: 1px;
		height: 20px;
		background-color: #e6394660;
	}

	.diamond {
		width: 12px;
		height: 12px;
		background-color: #e63946;
		transform: rotate(45deg);
		animation: pulse-diamond 2s ease-in-out infinite;
		box-shadow: 0 0 15px #e6394650;
	}

	@keyframes pulse-diamond {
		0%, 100% {
			transform: rotate(45deg) scale(1);
			opacity: 1;
			box-shadow: 0 0 15px #e6394650;
		}
		50% {
			transform: rotate(45deg) scale(1.1);
			opacity: 0.8;
			box-shadow: 0 0 25px #e63946;
		}
	}

	.role {
		display: flex;
		align-items: center;
		gap: 1rem;
		animation: role-reveal 1s ease-out forwards 1.4s;
		opacity: 0;
	}

	@keyframes role-reveal {
		0% { opacity: 0; transform: translateY(15px); }
		100% { opacity: 1; transform: translateY(0); }
	}

	.role-line {
		width: 30px;
		height: 1px;
		background: linear-gradient(90deg, #e6394660, #e63946);
		position: relative;
	}

	.role-line::after {
		content: '';
		position: absolute;
		top: 50%;
		transform: translateY(-50%);
		width: 4px;
		height: 4px;
		background: #e63946;
		border-radius: 50%;
		animation: role-dot-move 2s ease-in-out infinite;
	}

	.role:first-child .role-line::after {
		right: 0;
	}

	.role:last-child .role-line::after {
		left: 0;
	}

	@keyframes role-dot-move {
		0%, 100% { opacity: 0.5; }
		50% { opacity: 1; box-shadow: 0 0 6px #e63946; }
	}

	.role-text {
		font-size: 1rem;
		font-weight: 500;
		color: #888;
		letter-spacing: 0.3em;
		position: relative;
		padding: 0 0.5rem;
	}

	.role-text::before,
	.role-text::after {
		content: '';
		position: absolute;
		width: 4px;
		height: 4px;
		background: #e6394640;
		clip-path: polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%);
	}

	.role-text::before {
		left: -8px;
		top: 50%;
		transform: translateY(-50%);
	}

	.role-text::after {
		right: -8px;
		top: 50%;
		transform: translateY(-50%);
	}

	.scroll-line {
		width: 1px;
		height: 50px;
		background: linear-gradient(180deg, #e63946, #e6394650, transparent);
		position: relative;
	}

	.scroll-line::before {
		content: '';
		position: absolute;
		top: 0;
		left: 50%;
		transform: translateX(-50%);
		width: 6px;
		height: 6px;
		background: #e63946;
		clip-path: polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%);
		box-shadow: 0 0 8px #e63946;
	}

	.scroll-dot {
		width: 8px;
		height: 8px;
		background-color: #e63946;
		border-radius: 50%;
		animation: scroll-bounce 1.5s ease-in-out infinite;
		box-shadow: 0 0 10px #e6394680;
		position: relative;
	}

	.scroll-dot::after {
		content: '';
		position: absolute;
		top: 50%;
		left: 50%;
		transform: translate(-50%, -50%);
		width: 16px;
		height: 16px;
		border: 1px solid #e6394640;
		border-radius: 50%;
		animation: scroll-ring 1.5s ease-in-out infinite;
	}

	@keyframes scroll-ring {
		0%, 100% {
			transform: translate(-50%, -50%) scale(1);
			opacity: 0.5;
		}
		50% {
			transform: translate(-50%, -50%) scale(1.5);
			opacity: 0;
		}
	}

	@keyframes scroll-bounce {
		0%, 100% {
			transform: translateY(0);
			opacity: 1;
			box-shadow: 0 0 10px #e6394680;
		}
		50% {
			transform: translateY(8px);
			opacity: 0.5;
			box-shadow: 0 0 20px #e63946;
		}
	}

	.scroll-text {
		font-size: 0.7rem;
		font-weight: 500;
		color: #666;
		letter-spacing: 0.2em;
		margin-top: 0.25rem;
		position: relative;
	}

	.scroll-text::before,
	.scroll-text::after {
		content: '>';
		position: absolute;
		color: #e6394650;
		font-size: 0.6rem;
		animation: scroll-arrow 1s ease-in-out infinite;
	}

	.scroll-text::before {
		left: -15px;
		transform: rotate(90deg);
	}

	.scroll-text::after {
		right: -15px;
		transform: rotate(90deg);
		animation-delay: 0.5s;
	}

	@keyframes scroll-arrow {
		0%, 100% { opacity: 0.3; }
		50% { opacity: 0.8; }
	}

	@media (max-width: 768px) {
		main {
			gap: 2rem;
			padding: 1rem;
		}

		.name-container {
			margin-left: 0;
		}

		.info-section {
			margin-left: 0;
		}

		.scroll-indicator {
			left: 50%;
		}

		.first-name {
			font-size: 3.5rem;
		}

		.first-name::after {
			content: none;
		}

		.divider {
			width: 3px;
			margin-left: 0.5rem;
		}

		.divider::before,
		.divider::after {
			width: 8px;
			height: 8px;
		}

		.last-name {
			font-size: 1.5rem;
			letter-spacing: 0.2em;
		}

		.decoration-line {
			transform: scale(0.8);
		}

		.line {
			width: 20px;
		}

		.role-text {
			font-size: 0.75rem;
			letter-spacing: 0.15em;
		}

		.corner {
			width: 40px;
			height: 40px;
		}

		.corner::before {
			width: 6px;
			height: 6px;
		}

		.glow-orb {
			display: none;
		}
	}
</style>
